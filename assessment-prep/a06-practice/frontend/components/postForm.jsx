var React = require('react');
var PostActions = require('../actions/postActions.js');

module.exports = React.createClass({
  render: function() {
    return (
      <form>
        // Code goes here. Don't remove the form.
      </form>
    )
  }
});
